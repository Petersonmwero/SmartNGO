# Production dependencies (superset of base).
# Install: pip install -r requirements/production.txt
-r base.txt

# Add production-only packages here as needed, e.g.:
# gunicorn==21.2.0
# sentry-sdk==1.x
